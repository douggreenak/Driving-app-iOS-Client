// Template for Secrets.xcconfig. Copy this file to `Secrets.xcconfig` (which is
// gitignored) and replace the placeholder with the real key. The value must
// match the API_KEY environment variable set on the web backend (Vercel).
API_KEY = your-api-key-here
