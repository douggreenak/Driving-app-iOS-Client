import WidgetKit
import SwiftUI

/// Widget bundle for the drive Live Activity. This is the extension's entry point.
@main
struct DriveActivityBundle: WidgetBundle {
    var body: some Widget {
        DriveActivityLiveActivity()
    }
}
